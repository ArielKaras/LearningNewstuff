name: Continuous Integration

on: push

jobs:
  job1:
    name: First Job
    runs-on: ubuntu-latest
    steps:
      - name: step one
        uses: actions/checkout@v2
        run: echo "Hello World"